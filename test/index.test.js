describe('Sanity check', () => {
    it('Two plus two equals four', () => {
        const four = 2 + 2;
        expect(four).toEqual(4);
    });

    it('Apple is equal to apple', () => {
        const apple = "apple";
        expect(apple).toEqual("apple");
    });
});

